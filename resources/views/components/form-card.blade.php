<div class="card custom-card my-4">
    <div class="card-body p-5">
        <p class="h4 mb-2 fw-semibold">{{ $title }}</p>
        <p class="mb-4 text-muted">{{ $subtitle }}</p>
        {{ $slot }}
    </div>
</div>
